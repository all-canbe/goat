# 长线维护记录 — RGoat

> **使用说明**：每个长线维护项目在根目录创建此文件，随项目迭代持续更新。阶段完成后立即填写，问题出现即记录。该文档是团队交接、回溯、新人上手的第一入口。

---

## 一、项目概述

### 1.1 项目目标

将 Python Goat（本地 AI 编码助手 Agent）用 Rust 重构，从 ~200MB Electron 桌面应用变为 15-30MB 纯 Rust 二进制，同时增强向量记忆和代码语义搜索能力。

### 1.2 关键指标

| 指标 | 目标值 | 当前值 | 备注 |
|------|--------|--------|------|
| 二进制大小 | ≤ 30MB | 暂未发版 | 当前仅 core 库编译通过 |
| 内存占用 | ≤ 150MB | 未测量 | Python 原版 300-500MB |
| 编译通过 | 零错误零 warning | ✅ 通过 | cargo build 全 workspace (3 crate) |
| 功能覆盖率 | 对标 Goat v0.1.0 | ~90% | 核心引擎+TUI+React Desktop 前端，剩余: 本地向量/嵌入/代码索引真接入 |

### 1.3 技术栈 / 依赖

| 层级 | 选型 | 版本 | 用途 |
|------|------|------|------|
| 语言 | Rust | 1.96.0 (edition 2021) | 主语言 |
| 异步运行时 | tokio | 1.52 | 全异步架构 |
| LLM 协议 | reqwest + serde | 0.12 / 1.0 | OpenAI 兼容 API 调用 |
| 数据库 | SQLite (sqlx) | 0.8 | 对话持久化 |
| 向量记忆 | Zvec | 待接入 | 替换当前 InMemory 实现 |
| 本地嵌入 | Candle / ONNX Runtime | 待接入 | 离线嵌入 |
| TUI 框架 | ratatui + crossterm | 0.29 / 0.28 | 终端 UI |
| 桌面壳 | Tauri v2 | 2.x | GUI + 系统托盘 + 全局快捷键 |
| 包管理 | Cargo | 1.96.0 | workspace: rgoat-core + rgoat-tui + rgoat-desktop |
| 目标平台 | Windows 10+ | — | 优先支持，后续扩展 Linux/macOS |

### 1.4 仓库 / 文档链接

- 源码: `E:/WorkBuddyOutput/2026-06-25-10-54-30/goat_rust/`
- 原项目（Python Goat）: `E:/WorkBuddyOutput/2026-06-25-10-54-30/my-tui-main/`
- 重构方案文档: `E:/WorkBuddyOutput/2026-06-25-10-54-30/rgoat-refactoring-plan.md`
- 技术栈研究: `E:/WorkBuddyOutput/2026-06-25-10-54-30/windows-rust-agent-design.md`

---

## 二、阶段记录

### 阶段一：核心骨架搭建

**时间**：2026-06-25

**做了什么**：

- 创建 Cargo workspace，初始化 `rgoat-core` lib crate
- 实现 6 个核心模块：`core/`、`provider/`、`tools/`、`security/`、`conversation/`、`memory/`
- 创建 `agent/`、`mcp/`、`hooks/`、`tasks/`、`cli/` 模块骨架

**核心逻辑**：

架构分为三层：**基础设施层**（EventBus 广播通信、CancellationToken 取消、Workspace 路径管理、TokenTracker 成本追踪、Config 设置读写）→ **功能层**（LlmProvider trait 抽象 + OpenAI/Anthropic 双 Provider、Tool trait + ToolRegistry 工具注册表、ApprovalEngine 五层防御、SQLite 会话持久化 + 四层压缩、VectorStore trait + InMemory fallback）→ **编排层**（Agent ReAct 循环、MCP 双向协议、Hook 生命周期、Task 后台调度、CLI 命令解析）。所有跨模块通信通过 Arc<EventBus> 解耦，内存/嵌入/向量存储均使用 trait + fallback 模式确保无外部依赖也能编译。

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| LLM Provider 抽象 | trait 对象 / 泛型 | `Arc<dyn LlmProvider>` trait 对象 | 需要运行时多态，支持动态切换 Provider（DeepSeek→Claude→本地） |
| 向量存储 | Zvec 直接依赖 / trait + fallback | trait + InMemory fallback | Zvec Rust crate 尚未在 crates.io 发布，确保无外部依赖仍可编译 |
| 工具注册 | 静态注册 / 动态注册 | `HashMap<String, Arc<dyn Tool>>` 动态注册 | 支持 Skill 动态加载和 MCP 外部工具引入 |
| 审批引擎 | 硬编码规则 / 策略链 | 五层规则链（YOLO→策略→扩展→类别→默认） | 可扩展 + 默认安全，YOLO 模式下仍免疫破坏性操作 |
| 代码索引 | tree-sitter 编译时 / 启发式 | 启发式解析（fn/struct/enum 等关键字） | 避免 tree-sitter 编译复杂度和包体积膨胀 |

**产出物**：

- `goat_rust/rgoat-core/` — 11 个模块，~3500 行 Rust
- `goat_rust/Cargo.toml` — workspace 配置

---

### 阶段二：Agent 引擎 + MCP + 后台任务

**时间**：2026-06-25

**做了什么**：

- 实现完整的 ReAct Agent 循环（思考→行动→观察）
- 实现子 Agent 并行调度（Semaphore 控制 10 并发/3 深度）
- 实现 Flow 流水线（实现→审查→修复，最多 3 轮）
- 实现 MCP 协议的 Client/Server 双向支持
- 实现 Hook 生命周期系统、后台 TaskScheduler
- 实现 CLI 参数解析和交互式命令

**核心逻辑**：

Agent 启动后进入 ReAct 循环：从 ConversationManager 读取历史 → 构建 System Prompt + 消息 → 调用 LLM（带 ToolDef） → 解析返回的工具调用 → 经 ApprovalEngine 五层检查 → 执行工具 → 将结果（带 tool_call_id）写入 DB → 重复直到得到最终答案或达到 max_steps。子 Agent 通过 spawn 并行执行，Semaphore 控制并发数，超深递归直接拒绝。Flow 模式额外增加 git diff + review agent 验证步骤。

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| 工具调用 ID 传递 | 用 DB tool_call_id 字段 / 内存在线 | DB 字段 | LLM API 要求 tool 消息必须引用原始 tool_call_id，否则无法关联 |
| 事件类型映射 | 统一用一种 EventType / 按语义分类 | 按语义分类 | 不同事件类型（Thought/ToolCall/Approval 等）对应不同 UI 展示和监控 |
| 子 Agent 深度控制 | 硬编码 / 从 AgentConfig 读取 | 从 AgentConfig 读取 | 用户可通过配置自定义 max_depth，默认 3 |
| EventBus 通信 | 直接方法调用 / 广播 | 广播（tokio::broadcast） | 解耦 Agent 与 UI，支持多订阅者（TUI + 日志 + 指标） |
| MCP 传输 | 仅 stdio / stdio + SSE | stdio 优先，预留 SSE 接口 | stdio 是 MCP 规范的最基础传输，SSE 适合远程场景 |

**产出物**：

- `rgoat-core/src/agent/` — 4 文件（types + react + subagent + flow）
- `rgoat-core/src/mcp/` — 3 文件（types + client + server）
- `rgoat-core/src/hooks/mod.rs`、`tasks/mod.rs`、`cli/mod.rs`
- 修复：阶段一 9 个编译错误 → 零错误零 warning

---

### 阶段三：可执行 TUI 程序

**时间**：2026-06-25

**做了什么**：

- 创建 `rgoat-tui` 二进制 crate，输出 `rgoat.exe`
- 实现 ratatui TUI：消息历史滚动 + 响应区 + 输入行 + 斜杠命令
- 补齐缺失的 `OpenAiCompatibleProvider` 实现（基于 reqwest）
- 补充 `lib.rs` 重导出以支持下游 crate
- 代码审查发现并修复 4 个逻辑问题

**核心逻辑**：

TUI 在 tokio 主循环中轮询 crossterm 按键事件，回车提交消息时 spawn Agent 异步任务，UI 通过 EventBus 订阅获取流式输出。Layout 分四栏：标题栏（版本+会话ID+模式）、消息历史（滚动列表）、响应区（流式输出）、输入行（支持光标移动和中文输入）。斜杠命令 `/help` `/clear` `/mode` `/exit` 本地处理。

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| TUI vs GUI 优先 | ratatui / Tauri | ratatui 先做 | 终端 UI 开发迭代快，无需 WebView 依赖，Tauri 作为后续可选项 |
| Agent 异步集成 | 阻塞主线程 / spawn 异步 | spawn 异步 | 避免 TUI 在 Agent 执行时冻结，同时保持按键响应 |
| Provider 实现位置 | 放在 rgoat-core / rgoat-tui | rgoat-core | 其他下游（如 MCP server）也需要 LLM 调用能力 |
| API Key 管理 | 硬编码 / 环境变量 + settings.json | 环境变量优先 + settings.json 兜底 | 环境变量适合 CI/自动化，settings.json 适合个人日常使用 |

**产出物**：

- `goat_rust/rgoat-tui/` — 二进制 crate，~500 行
- `goat_rust/rgoat-core/src/provider/impls.rs` — OpenAI 兼容 Provider
- 代码审查报告，4 个逻辑问题全部修复
- 全 workspace `cargo build` 零错误零 warning

### 阶段三补充：TUI Agent 真正集成（5项补齐）

**时间**：2026-06-26

**做了什么**：

- **Agent 结果流式渲染**：TUI 通过 `broadcast::Receiver` 订阅 EventBus，实时解码 `AgentEvent`→`UiLine` 并渲染到消息区
- **工具调用卡片展示**：工具调用开始时显示 🔧 图标+名称，执行完毕显示 ✓/✗ + 结果摘要+行数
- **审批状态展示**：🛡 图标显示审批决策（Allow/Block/Ask），工具名+decision
- **会话管理 UI**：`/new` `/list` `/mode` 斜杠命令 + 顶部标题栏实时显示 session ID 和模式
- **Plan 模式界面支持**：模式在标题栏实时显示，`/mode plan` 切换模式（需重启生效）

**核心逻辑**：

TUI 事件循环用 `try_recv()` 非阻塞轮询 EventBus 的 broadcast receiver，每轮先排空所有 Agent 事件更新 App 状态，再调用 `terminal.draw()` 渲染。Agent 通过 `tokio::spawn` 异步运行，通过 EventBus 解耦——Agent 不知道 TUI 的存在，TUI 只是 EventBus 的另一个订阅者。每条 AgentEvent 被解码为 `UiLine` 枚举（User/Assistant/Thought/ToolStart/ToolResult/Approval/System/Error），每个变体有独立的颜色和图标渲染。

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| Agent→TUI 通信 | EventBus 广播 / 专用 mpsc | EventBus 广播 | 复用已有基础设施，TUI 只是一个订阅者，无需额外通道 |
| 事件轮询方式 | tokio::select! / try_recv | try_recv 非阻塞 | 避免 select 的复杂度，crossterm 同步 poll 与 tokio 异步可共存 |
| 审批交互 | 阻塞等待用户确认 / 自动处理 | MVP 阶段自动处理 | 双向 oneshot 通道增加复杂度，后续迭代再做交互式审批 |
| 消息渲染 | 原始文本 / 结构化卡片 | 图标+颜色+摘要 | Agent 输出量大时结构化显示更可读，工具调用/审批一目了然 |

**产出物**：

- `rgoat-tui/src/app.rs` 完全重写（~450 行），`rgoat-tui/src/main.rs` 适配新 API
- 全 workspace `cargo build` 零错误零 warning

### 阶段四：桌面应用（Tauri v2）

**时间**：2026-06-29

**做了什么**：

- 创建 `rgoat-desktop` Tauri v2 桌面应用 crate
- 系统 WebView 前端：暗色主题聊天界面，8 种消息样式，Provider 下拉切换
- Rust 后端 IPC：5 个 Tauri 命令 + EventBus→前端事件桥接
- 系统托盘 + 插件（通知/剪贴板/自启动/对话框）
- 生成占位图标（多分辨率 PNG + ICO + ICNS）

**核心逻辑**：

Tauri v2 架构分三层：**WebView 前端**（index.html + 纯 JS，无框架）→ **Tauri IPC 桥**（`invoke()` 调用 Rust 命令，`listen("agent-event")` 接收流式事件）→ **rgoat-core**（Agent + EventBus + ProviderSwitch）。Agent 后台运行通过 `tokio::spawn`，结果通过独立线程订阅 EventBus 并用 `app.emit()` 推送到前端。前端解析 `AgentEvent` 并按类型（user/assistant/thought/tool-start/tool-result/approval/error）渲染为不同样式的消息卡片。

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| 前端技术 | React / 纯 HTML | 纯 HTML/CSS/JS | 零构建步骤，Tauri 直接加载静态文件，开发迭代快 |
| Agent→前端通信 | invoke 轮询 / 事件推送 | Tauri 事件（emit→listen） | 前端无需轮询，EventBus 桥接线程实时推送，延迟低 |
| Provider 管理 | 独立线程 / Tauri State | Tauri State | 与 TUI 共享 `ProviderSwitch` 架构，运行时切换一致 |
| 托盘功能 | 预置 / 后续添加 | 基础托盘（仅图标） | MVP 聚焦核心功能，托盘菜单后续迭代 |
| 自启动插件 | 默认参数 | `MacosLauncher::LaunchAgent` + 空参数 | Windows 下参数为启动标志 |

**产出物**：

- `rgoat-desktop/` — 完整 Tauri v2 项目（Cargo.toml + tauri.conf.json + capabilities + build.rs + icons + index.html + style.css + app.js + src/）
- 全 workspace `cargo build` 零错误零 warning（含 Tauri 编译）

### 阶段四补充一：配置向导 + Provider 自动识别

**时间**：2026-06-29

**做了什么**：

- 为 TUI 和 Desktop 都添加了首次启动配置向导
- TUI: `run_setup_wizard()` 4 步交互（Base URL → 自动识别类型 → API Key → Model → 存储名）
- Desktop: WebView Setup Screen 表单，含 URL 自动检测和保存
- Provider 自动识别：URL 含 "anthropic" 子串 → Anthropic Messages API；否则 → OpenAI Compatible
- 运行时 Provider 切换：`/provider` `/providers` 斜杠命令 + Desktop 下拉菜单
- 配置持久化到 `%APPDATA%/goat/setting.json`

**核心逻辑**：

`Settings::detect_provider_type(url)` 通过 URL 子字符串匹配判断 API 格式。`ProviderSwitch` 包装 `HashMap<String, Arc<dyn LlmProvider>>`，通过 `select(name)` 运行时切换，Agent 无感知。配置以 `ProviderSettings` 列表存储，每个 provider 含 `name/base_url/api_key/models/provider_type`。

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| API 格式识别 | 用户手动选择 / URL 自动检测 | URL 自动检测 | 减少用户认知负担，含 "anthropic"=Anthropic，否则=OpenAI Compatible |
| API Key 存储 | 明文 config / 系统凭据保管箱 | 明文 settings.json | 本地桌面应用，安全需求低，简单直接 |
| 首次运行检测 | has_configured_provider() / 强制向导 | has_configured_provider() 检查 | 环境变量有 API Key 跳过向导，0 摩擦启动 |

**产出物**：
- `rgoat-core/src/core/config.rs` 新增 `add_or_update_provider()`、`detect_provider_type()`、`load_or_empty()`、`has_configured_provider()`
- `rgoat-core/src/provider/switch.rs` 新增 `ProviderSwitch` 完整实现 + `ProviderDetail`
- `rgoat-tui/src/main.rs` 新增 `run_setup_wizard()`
- `rgoat-desktop/src/commands.rs` 新增 `configure_provider` 命令

### 阶段四补充二：Desktop UI 改进计划 + Phase 1 紧急修复

**时间**：2026-06-29

**做了什么**：

- 撰写 `UI_IMPROVEMENT_PLAN.md`：完整的四阶段改进路线，含功能对比矩阵、组件规格、优先级排序
- Phase 1 紧急修复 — 使用 2 个子 Agent 并行修复：
  - **Agent 1 (Rust)**：事件桥接扁平化，`{eventType, source, data}` → `{type, source, ...AgentEvent字段}`
  - **Agent 2 (Frontend)**：`dist/index.html` 完全重写（952 行），流式累积、Markdown、工具卡片、审批弹窗、Setup 向导

**核心逻辑**：

事件桥接格式匹配问题的根因：`AgentEvent` 已通过 `#[serde(tag = "type", rename_all = "snake_case")]` 序列化，`event.data` 中已含 `{"type": "thought", ...}`。Bridge 层错误地再嵌套一层 `{eventType, source, data}`。修复为：展开 `event.data` 的 Object map，插入 `source` 字段后直接 emit。

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| 事件格式 | 双层嵌套 / 扁平化 | 扁平化 | JS 端直接读 `payload.type`，零解析成本 |
| 前端实现 | Vanilla JS / React | Phase 1 用 Vanilla，Phase 2 转 React | Phase 1 目标只是让 UI 能用，引入 React 会增加构建复杂度 |

**产出物**：
- `rgoat-desktop/src/lib.rs` 事件桥接修复
- `rgoat-desktop/dist/index.html` Phase 1 重写版（952 行）
- `rgoat-desktop/UI_IMPROVEMENT_PLAN.md`

### 阶段四补充三：Phase 2 — Vite + React 前端重构

**时间**：2026-06-29

**做了什么**：

在修复完 3 个后端阻塞性 Bug（见问题记录）后，使用 2 个子 Agent 完成 Phase 2 前端重构：

- **Agent 1 (Scaffolding)**：搭建 `rgoat-desktop/frontend/` Vite + React 18 + TypeScript + Tailwind + Zustand 项目
- **Agent 2 (Components)**：实现完整组件架构

**核心逻辑**：

前端架构分为 4 层：**工具层**（`tauri-bridge.ts` 封装 Tauri IPC 调用和 AgentEvent 类型）→ **状态层**（3 个 Zustand Store：`chatStore` 消息+流式状态+审批数据，`sessionStore` 会话 CRUD，`configStore` Provider 管理）→ **Hook 层**（`useAgentEvents.ts` 订阅 agent-event，路由到各 Store）→ **组件层**（18 个 React 组件，覆盖 Sidebar/Chat/Input/Approval/Setup/Status）。

组件树：`App → SetupScreen | AppLayout → Sidebar + ChatArea(MessageList + InputPanel) + StatusBar + ApprovalDialog`

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| 状态管理 | Context / Zustand | Zustand | 轻量（<1KB），与 Goat Web 原版一致，支持组件外访问 |
| Markdown 渲染 | marked / react-markdown | react-markdown + remark-gfm + rehype-highlight | 与 Goat Web 原版一致，代码高亮复用生态 |
| 图标方案 | emoji / lucide-react | lucide-react | 干净专业，与 Goat Web 原版一致 |
| Tauri 运行方式 | devUrl 热更新 / 纯静态构建 | 纯静态（npm run build + cargo run） | 避免 devUrl 导致的"无法访问此页面"，`cargo run` 稳定可用 |
| 样式方案 | CSS Modules / Tailwind | Tailwind | 与 Goat Web 原版一致，class 可直接复用 |

**产出物**：

- `rgoat-desktop/frontend/` — 18 个组件文件 + 3 个 Store + 1 个 Hook
- `rgoat-desktop/tauri.conf.json` — 更新 `frontendDist` 指向 `frontend/dist`
- 旧版 `rgoat-desktop/dist/` 归档到 `rgoat-desktop/archive/`
- `npm run build` ✅ + `cargo build` ✅，零错误零 warning

---

### 阶段五：沙箱安全增强 + 桌面端生产化（D1-D3）

**时间**：2026-07-16

**做了什么**：

本阶段分两条主线推进：**安全防线加固**（沙箱从独立模块到 Agent 集成）与**桌面端对标生产级工具**（D1 审批升级 → D2 项目记忆/Plan Mode → D3 稳定性/持久化/Skill 系统）。

#### 5.1 沙箱安全增强

- **Linux Landlock + seccomp 双层内核防护**：采用 `extrasafe` crate 统一封装 Landlock（文件系统访问控制）+ seccomp（syscall 过滤），替代原方案的 bubblewrap。内核不支持时优雅降级到应用层路径检查。
- **跨平台沙箱矩阵**：Windows（Job Objects + 应用层路径检查）/ Linux（Landlock + seccomp + 应用层兜底三层）/ 其他（NoopSandbox 应用层路径检查）。
- **沙箱集成到 Agent 执行路径**：`ReActAgent` 新增 `sandbox: Option<Arc<dyn Sandbox>>` 字段 + `with_sandbox()` builder + `check_sandbox_allowed()` 方法，在 4 个工具执行点（Yolo 放行 / 持久化规则命中 / 会话缓存命中 / 用户审批通过）全部添加路径校验，形成"审批系统 + 沙箱路径校验 + 工具内置 safe_path"三层 defense-in-depth。
- **仅校验文件操作工具**（read_file/write_file/edit_file），Shell/Git 等路径参数复杂的工具不校验，避免误判。

#### 5.2 D1：审批系统升级 + Changes 面板

- **ApprovalDialog 重写**：max-w-2xl 容器，按 tool_type 分视图（Write/Shell/Network/Generic），Header 含风险徽章 + danger_score 进度条，Footer 含 ApprovalScopeSelector（Once/Session/AllSimilar/Always 4 档）+ Deny/Approve 按钮，键盘快捷键 Y/A/S/W/N/Esc。
- **关键 bug 修复**：前端监听 `"approval"` 但后端 emit `"approval_required"`（serde rename_all snake_case），导致审批弹窗永不触发。
- **Changes 面板**：Sidebar 新增 Changes Tab，展示会话内文件变更（ChangeItem + DiffViewer + DiffSummary）。
- **StatusBar 统计**：新增 pendingApprovals/totalChanges/toolCallCount 三个统计区域。
- **P1 修复**：Esc 从 `setApproval(null)` 改为 `respond(false, "once")`，避免 Agent 永久挂起；Y 快捷键添加 allow_options 检查。

#### 5.3 D2：项目记忆 + Plan Mode 集成

- **项目记忆加载**：`load_rules` 新增 CLAUDE.md/GOAT.md/AGENTS.md 加载，与 Claude Code / OpenCode 生态兼容。
- **send_prompt 启用 mode 字段**：前端传递 mode（agent/plan/flow/yolo），后端调用 `set_mode`。
- **Plan Mode 提示**：InputPanel placeholder 动态化，AppLayout 添加黄色提示条。

#### 5.4 D3：稳定性 + 持久化 + Skill 系统

- **D3-T01 SyncMode UB 修复**：`UnsafeCell<AgentMode>` + `unsafe impl Sync`（不 sound）改为 `AtomicUsize` + `std::mem::transmute` + `#[repr(usize)]`，新增 `get_mode()` 方法。
- **D3-T01 Flow 重复加载修复**：`flow.rs` 的 `run` 与 `run_fix` 方法中 `load_project_context` 替换为 `ReActAgent::load_rules(workspace).join("\n\n")`，删除孤儿函数 `load_project_context`。
- **D3-T02 嵌套项目记忆**：`load_rules` 第 6 步扩展为从 cwd 递归向上查找 GOAT.md/CLAUDE.md/AGENTS.md/.goat/rules.md，支持 monorepo/子目录工作场景。
- **D3-T03 Always 持久化**：新增 `PersistentApprovalRule { tool_name, args_hash }` 结构 + `load_persistent_rules`/`save_persistent_rules` 函数，持久化到 `<workspace>/.goat/approval_rules.json`。session_cache 查询前检查持久化规则，Always 决策写入持久化文件，实现跨会话免审批。
- **D3-T04 Plan Mode 落地**：`send_prompt` 在 Plan Mode 下调用 `PlanRunner::new(agent, workspace, sid).run(&prompt)`，复用已实现的两阶段流程（Explore → Plan）。
- **D3-T05 Skill 系统**：后端新增 `SkillInfo` 结构 + `load_skills_structured` + `read_skill_content` + `list_skills`/`read_skill` IPC；前端新增 `skillStore.ts` + InputPanel Slash 命令下拉（`/help`、`/clear`、`/skills`、`/<skill-name>`），`/<skill-name>` 读取完整 SKILL.md 作为上下文注入发送给 Agent。
- **项目记忆优先级调整**：全代码库统一为 **GOAT.md（大写）> CLAUDE.md > AGENTS.md**，原 goat.md 改名为大写 GOAT.md 并提升到最高优先级。

**核心逻辑**：

安全防线从"审批系统单层"升级为"审批系统 + 沙箱路径校验 + 工具内置 safe_path"三层 defense-in-depth。Agent 工具执行流程：ApprovalEngine 五层检查（YOLO→策略→扩展→类别→默认）→ 沙箱 `is_path_allowed()` 路径校验 → 工具内部 `safe_path()` 解析。Linux 平台额外有 Landlock 内核级强制 + seccomp syscall 过滤。

桌面端从"能用"升级为"接近生产级"：审批弹窗按工具类型分视图 + 4 档授权范围 + diff 预览；文件变更可视化；项目记忆支持 GOAT.md/CLAUDE.md/AGENTS.md + 嵌套递归查找；Plan Mode 两阶段流程落地；Skill 系统支持 Slash 命令快速调用；Always 审批跨会话持久化。

**关键决策**：

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| Linux 沙箱方案 | bubblewrap / landlock+libseccomp / extrasafe | extrasafe | 统一 API 封装 Landlock + seccomp，减少依赖复杂度，优雅降级 |
| 沙箱集成方式 | sandbox.init() 线程级 / 应用层路径检查 | 应用层路径检查 | apply_to_current_thread 会限制 tokio worker 线程，应用层检查更安全 |
| SyncMode 实现 | UnsafeCell + unsafe Sync / AtomicUsize | AtomicUsize + transmute | 原 UB 不 sound，原子枚举存储是标准模式 |
| 项目记忆优先级 | CLAUDE.md > goat.md / GOAT.md > CLAUDE.md | GOAT.md > CLAUDE.md > AGENTS.md | Goat 原生规范优先，兼容 Claude Code 与 OpenCode 生态 |
| Always 持久化位置 | DB / JSON 文件 | JSON 文件（.goat/approval_rules.json） | 无需 DB schema 变更，按 workspace 隔离，可读可调试 |
| Skill 加载策略 | 首轮全量注入 / 摘要注入 + 按需加载 | 摘要注入 + 按需加载 | system prompt 只注入 name+description，Slash 命令触发时读取完整内容，对标 ECC Skill-First |
| Slash 命令分发 | 全部发送给 Agent / 本地分发 | 本地分发（/help /clear /skills 本地，/<skill> 加载后发送） | 纯 UI 操作无需走 Agent，减少延迟与 token 消耗 |

**产出物**：

- `rgoat-core/src/security/sandbox.rs` — Linux Landlock + seccomp 实现 + 跨平台工厂
- `rgoat-core/src/agent/react.rs` — 沙箱集成 + SyncMode AtomicUsize + load_rules 嵌套查找 + Always 持久化 + SkillInfo/load_skills_structured/read_skill_content
- `rgoat-core/src/agent/flow.rs` — run/run_fix 改用 load_rules + 删除 load_project_context
- `rgoat-core/src/security/approval.rs` — AgentMode `#[repr(usize)]` + PersistentApprovalRule + load/save 函数
- `rgoat-core/tests/sandbox_test.rs` — 8 个跨平台测试 + 4 个 Linux 专用测试
- `rgoat-core/tests/sandbox_integration.rs` — 4 个全链路集成测试
- `rgoat-desktop/src/commands.rs` — send_prompt Plan Mode 集成 + list_skills + read_skill IPC
- `rgoat-desktop/src/lib.rs` — 注册 list_skills + read_skill 命令
- `rgoat-desktop/frontend/src/stores/skillStore.ts` — 新建 Skill store
- `rgoat-desktop/frontend/src/stores/changesStore.ts` — 新建文件变更 store
- `rgoat-desktop/frontend/src/components/input/InputPanel.tsx` — Slash 命令下拉 + 命令分发
- `rgoat-desktop/frontend/src/components/dialogs/ApprovalDialog.tsx` — 重写（按 tool_type 分视图 + 4 档 scope + diff 预览）
- `rgoat-desktop/frontend/src/components/dialogs/ApprovalScopeSelector.tsx` — 新建
- `rgoat-desktop/frontend/src/components/sidebar/ChangesPanel.tsx` + `ChangeItem.tsx` — 新建
- `rgoat-desktop/frontend/src/components/status/StatusBar.tsx` — 新增统计区域
- `rgoat-desktop/frontend/src/hooks/useAgentEvents.ts` — 修复事件名 bug + 补全事件分支

**验证**：

- `cargo check --workspace`：exit 0（仅 rgoat-tui 既有 warnings）
- `cargo test -p rgoat-core --lib`：78 passed, 0 failed
- `cargo test -p rgoat-core --test sandbox_test`：8 passed
- `cargo test -p rgoat-core --test sandbox_integration`：4 passed
- `npm run build`：exit 0

---

## 三、问题记录

### 已记录问题

#### 2026-06-25 | [逻辑] ReAct 循环中 tool_call_id 丢失

**现象**：Agent 执行工具后将结果写入 DB 时未保存 tool_call_id，下一轮 LLM 收到 tool 消息但无法匹配到原始工具调用，导致 API 报错或行为异常。

**根因**：`add_observation` 方法的签名中缺少 `tool_call_id` 参数，`handle_tool_result` 调用时未传递 LLM 返回的 `ToolCallDef.id`。

**临时处理**：Phase 3 审查时发现并修复，无需线上止血。

**长期修复**：`add_observation` 增加 `tool_call_id: &str` 参数，`ConversationManager::add_message` 的 `tool_call_id` 参数设为 `Some(tool_call_id)`。

**如何避免再次发生**：
- Agent 与 DB 交互的每一次写入必须检查 tool_call_id 字段完整性
- 编写集成测试覆盖完整的 ReAct 循环（含多轮工具调用）
- CR 时关注 OpenAI API 对 tool message 的协议要求

---

#### 2026-06-25 | [逻辑] EventBus 事件类型全部用 ToolCallResult

**现象**：Agent 的 `emit` 方法无论什么事件类型（Thought/ToolCall/Approval/Finished）都发 `EventType::ToolCallResult`，导致 UI 层无法区分显示。

**根因**：快速原型阶段未做事件类型映射。

**临时处理**：Phase 3 审查时修复。

**长期修复**：在 `emit` 方法中按 `AgentEvent` 变体 match 到对应的 `EventType`：
- `Started` → `SubAgentSpawned`
- `Thought` → `LlmStreamChunk`
- `ToolCall` → `ToolCallStart`
- `ToolResult` → `ToolCallResult`
- `Approval` → `ApprovalDecided`
- `Finished` → `LlmStreamDone`
- `Error` → `Error`

**如何避免再次发生**：
- EventBus 的 `EventType` 枚举与 `AgentEvent` 变体应保持一一对应
- 添加 CI 检查确保每个 AgentEvent 变体都有对应的 EventType 映射

---

#### 2026-06-25 | [配置] 子 Agent 深度硬编码 3 忽略 AgentConfig

**现象**：`SubAgentRuntime::new()` 中 `max_depth` 硬编码为 3，用户设置 `AgentConfig.max_subagent_depth = 5` 无效。

**根因**：使用字面量 `max_depth: 3` 而非 `agent.config.max_subagent_depth`。

**临时处理**：Phase 3 审查时修复。

**长期修复**：改为 `let max_depth = agent.config.max_subagent_depth;`

**如何避免再次发生**：
- AgentConfig 中每个字段都应有使用处的引用检查
- 避免在构造函数中使用硬编码默认值代替配置值

---

#### 2026-06-25 | [编译] 阶段一多类型未实现必要 trait

**现象**：首次 `cargo check` 出现 9 个编译错误，包括 `WindowsSandbox` 的 `*mut c_void` 不实现 `Send`、`ChatMessage` 缺少 `Default`、`AgentError` 无法自动 `Clone`、临时值生命周期问题等。

**根因**：阶段一在无 Rust 环境下手写，无法即时验证。

**临时处理**：安装 Rust 后逐项修复：
- `AgentError` 手动实现 `Clone`（`std::io::Error` 和 `serde_json::Error` 不自动 Clone）
- `ChatMessage` 添加 `Default` derive
- `StdioMcpClient` 的 stdin/stdout 用 `tokio::sync::Mutex` 包装
- 生命周期问题用 `let` 绑定和 `cloned()` 修复

**长期修复**：设置 CI（GitHub Actions）在每次 push 时运行 `cargo check --workspace`。

**如何避免再次发生**：
- 禁止在无编译验证的环境下手写跨多模块的代码
- 每完成一个模块立即编译验证

---

#### 2026-06-29 | [数据] Desktop send_prompt 将 session_id 当 title 写入，导致 FOREIGN KEY 错误

**现象**：Desktop 前端通过 `send_prompt` 发送消息后，Agent 执行工具调用时向 messages 表写入记录，报 `FOREIGN KEY constraint failed (code: 787)`，工具链中断。

**根因**：`commands.rs` 中 `send_prompt` 将前端传入的 `session_id` 作为 `create_session(Some(&session_id), ...)` 的 `title` 参数传入，而 `create_session` 内部生成新 UUID 作为真实 ID。Agent 后续用前端传入的 ID 写入 messages 表时，该 ID 在 sessions 表中不存在，外键约束失败。

**临时处理**：调用方已上线，暂无线上影响（Desktop 尚未发布）。

**长期修复**：在 `ConversationManager` 新增 `get_or_create_session(session_id, title, workspace)` 方法，先用传入的 session_id 查 sessions 表，存在则返回，不存在则用此 ID 插入。`commands.rs` 的 `send_prompt` 改为调用此方法。

**如何避免再次发生**：
- 任何数据库操作方法签名必须明确区分「生成本地 ID」和「接受外部 ID」两种语义
- 编写集成测试覆盖跨 Agent→DB 的完整消息写入流程

---

#### 2026-06-29 | [架构] configure_provider 保存后需重启，无法即时生效

**现象**：用户在 Desktop Setup Screen 填写 provider 配置后点击保存，应用提示「saved. Please restart」，用户必须手动重启才能使用。首次用户体验差。

**根因**：`ProviderSwitch.register()` 需要 `&mut self`（可变引用），但 AppState 中的 `switch` 是 `Arc<ProviderSwitch>`（不可变共享引用），`configure_provider` 命令无法调用 `register()`。只能保存到 settings.json，等下次启动 `init_providers()` 时从文件加载。

**长期修复**：
1. 重构 `ProviderSwitch` 内部使用 `RwLock<HashMap>` + `RwLock<Vec>`，所有方法改为 `&self`（异步方法保持 `async`，添加 `.await` 内部获取锁）
2. 为保持 TUI 和 Desktop 初始化阶段的兼容性，添加 `register_blocking(&mut self)` 同步版本
3. `configure_provider` 保存 settings.json 后，立即创建 `ProviderConfig` + provider 实例，调用 `register()` + `select()` 即时生效

**如何避免再次发生**：
- 提供「保存并立即启用」作为默认行为，而非「保存后需重启」
- 配置变更应触发运行时重新加载，而非依赖启动初始化

---

#### 2026-06-29 | [UI] 事件桥接层出现重复消息（最终答案显示两次）

**现象**：Desktop 前端收到 Agent 返回后，聊天区显示了两条相同的 assistant 消息。

**根因**：Agent 在 `react.rs` 中每步发出 `Thought` 事件（含 content_text），结束时发出 `Finished` 事件（含同一 content_text）。前端 JS 将 Thought 累积为流式内容，Finished 时调用 `commitStream()` 添加一次，再 `addAssistantMessage(answer)` 添加第二次。

**长期修复**：
1. 前端 `useAgentEvents` 的 `finished` case 中：先检查 `streamingContent` 是否与 `answer` 相同，若相同则不重复添加
2. 长期可在 Rust 端调整事件语义：`Thought` 和 `Finished` 不发送相同内容，或让 `Finished` 仅作为结束标记

**如何避免再次发生**：
- 前后端约定事件协议时明确每个事件的数据内容
- 集成测试中验证消息列表不包含重复内容

---

#### 2026-06-29 | [UI] ToolCallCard 参数在 tool_result 后丢失

**现象**：工具调用卡片在收到结果后，用户点击展开时看不到原始参数（arguments）。

**根因**：前端 `chatStore.updateToolResult` 将 `type: "tool_call"` 的消息整体替换为 `type: "tool_result"` 的新对象，新对象没有 `arguments` 字段。MessageBubble 对 `tool_result` 类型传递空对象给 ToolCallCard。

**长期修复**：改为在原消息对象上通过 `{ ...m, success, output }` 更新字段，保留 `arguments` 和 `toolName`。

**如何避免再次发生**：
- 状态更新应使用对象展开（spread）而非替换
- 添加 TypeScript 类型约束确保 message 对象的核心字段不可变

---

#### 2026-06-30 | [基础设施] ConversationManager 新增 new_with_path 支持自定义 DB 路径

**背景**：集成测试需要在临时目录创建独立的 SQLite 数据库，而非使用生产环境 `%APPDATA%/goat/conversations.db`。

**变更**：`ConversationManager` 新增 `new_with_path(db_path: &Path)` 方法，原有 `new()` 委托调用 `new_with_path`。

**参数**：`db_path` 为完整 SQLite 文件路径（含 .db 扩展名），方法内部自动创建父目录和初始化表结构。

---

#### 2026-06-30 | [测试] 核心对话-执行-工具链路集成测试

**测试文件**：`rgoat-core/tests/agent_integration.rs`（~420 行）

**测试用例**：

| 测试 | 验证内容 | 结果 |
|------|---------|------|
| `test_event_serde_tags_are_correct` | AgentEvent 的 serde `tag = "type"` 序列化格式正确 | ✅ |
| `test_simple_answer_no_tool_calls` | 无工具调用场景：Started→Thought→Finished 事件 + DB 写入 | ✅ |
| `test_full_react_loop_with_tool_call` | 完整 ReAct 循环：Mock LLM→read_file 工具→observation→最终答案，8 个 EventBus 事件 + 4 条 DB 消息 | ✅ |
| `test_tool_call_id_persists_in_db` | tool_call_id 正确写入 DB（避免 FOREIGN KEY 错误） | ✅ |

**测试架构**：
- 使用 `MockTwoRoundProvider` 模拟两轮 LLM 对话（第 1 轮返回工具调用，第 2 轮返回最终答案）
- 使用 `SingleAnswerProvider` 模拟无工具调用的简单回答
- 通过预先 `bus.subscribe()` 保证广播事件不丢失，再用 `rx.try_recv()` 非阻塞 drain
- 每个测试在独立的 `temp_dir()` 中创建 workspace + DB 文件，测试结束后清理

**关键发现**：
- `steps_taken` = 2（步进计数从 0 开始：第 0 步处理工具调用，第 1 步返回最终答案）
- 广播 channel 的 receiver 必须在 agent.run() 之前订阅，否则错失所有事件

**运行命令**：`cargo test -p rgoat-core --test agent_integration`

---

## 四、维护规范

### 4.1 操作检查清单

- [ ] 新模块是否通过 `cargo check --workspace`？
- [ ] 是否更新了 `lib.rs` 中的重导出？
- [ ] 新增 trait 是否需要对应的 Mock/Fallback 实现？
- [ ] 是否存在跨模块的接口变更（如方法签名修改）需同步更新下游？
- [ ] 是否更新了本文档（阶段记录 / 问题记录）？
- [ ] `cargo build --workspace` 是否零 error 零 warning？

### 4.2 编译与测试

| 检查项 | 命令 | 频率 |
|--------|------|------|
| 编译检查 | `cargo check --workspace` | 每模块完成时 |
| 完整构建 | `cargo build --workspace` | 每阶段完成时 |
| 单元测试 | `cargo test --workspace` | 4 个集成测试（agent_integration）✅ |
| Clippy | `cargo clippy --workspace` | 待接入 |
| 格式化 | `cargo fmt --check` | 待接入 |

### 4.3 周期性任务

| 任务 | 频率 | 备注 |
|------|------|------|
| 依赖版本更新 | 每阶段开始时 | 检查 crates.io 更新 |
| Zvec 接入 | Phase 4 | 当 Zvec Rust crate 可用时替换 InMemoryVectorStore |
| Candle 接入 | Phase 4 | 替换 MockEmbedder |
| Tauri 桌面壳 | Phase 5 | 可选的 GUI 入口 |
| 代码审查 | 每阶段结束 | 审查接口一致性、逻辑正确性、错误处理 |

### 4.4 项目结构速查

```text
goat_rust/
├── Cargo.toml                  ← workspace 配置
├── Cargo.lock
├── 长线维护记录.md              ← 本文件
├── rgoat-core/                 ← 核心库 crate
│   ├── Cargo.toml
│   └── src/
│       ├── lib.rs              ← 模块注册 + 重导出
│       ├── core/               ← EventBus, CancellationToken, Workspace, Config
│       ├── provider/            ← LlmProvider trait + impls + switch
│       ├── tools/               ← Tool trait + Registry + builtin
│       ├── security/             ← ApprovalEngine + Sandbox
│       ├── conversation/        ← ConversationManager + Compressor + Templates
│       ├── memory/               ← VectorStore + Embedding + CodeIndex
│       ├── agent/                ← ReActAgent + SubAgent + Flow
│       ├── mcp/                  ← MCP Client + Server
│       ├── hooks/                ← Hook 生命周期
│       ├── tasks/                ← 后台 TaskScheduler
│       └── cli/                  ← 参数解析 + 交互命令
├── rgoat-tui/                    ← TUI 二进制 crate
│   ├── Cargo.toml
│   └── src/
│       ├── main.rs               ← 入口（CLI 路由 + setup wizard + provider init）
│       └── app.rs                ← ratatui 界面（Agent 集成 + EventBus 订阅）
├── rgoat-desktop/                 ← Tauri v2 桌面应用
│   ├── Cargo.toml
│   ├── tauri.conf.json          ← Tauri 配置（frontendDist = frontend/dist）
│   ├── build.rs
│   ├── capabilities/            ← 权限声明
│   ├── gen/                     ← Tauri 自动生成的 schemas
│   ├── icons/                   ← 应用图标（多分辨率）
│   ├── src/
│   │   ├── main.rs              ← 入口
│   │   ├── lib.rs               ← Tauri setup + Agent 初始化
│   │   └── commands.rs          ← IPC 命令（9 个）
│   ├── frontend/                ← ★ 新：Vite + React 前端
│   │   ├── package.json
│   │   ├── vite.config.ts
│   │   ├── tsconfig.json
│   │   ├── tailwind.config.ts
│   │   ├── index.html
│   │   ├── dist/                ← 构建产物
│   │   └── src/
│   │       ├── main.tsx
│   │       ├── App.tsx
│   │       ├── index.css
│   │       ├── lib/             ← tauri-bridge.ts + utils.ts
│   │       ├── hooks/           ← useAgentEvents.ts
│   │       ├── stores/          ← chatStore + sessionStore + configStore
│   │       └── components/      ← 18 个组件（layout/chat/input/dialogs/setup/status）
│   ├── archive/                 ← ★ 归档：Phase 1 旧版单文件前端
│   │   ├── index.html
│   │   ├── app.js
│   │   └── style.css
│   └── UI_IMPROVEMENT_PLAN.md   ← Desktop UI 改进计划文档
└── target/                      ← Cargo 编译输出
```

---

## 附录：维护者

| 时间段 | 负责人 | 备注 |
|--------|--------|------|
| 2026-06 ~ 至今 | 深圳 Java 后端应届生 | 个人项目，Python Goat 原作者 |
