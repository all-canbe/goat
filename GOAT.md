一、思考先行
不臆测、不掩饰、暴露权衡。
在实现前：
明确陈述假设。不确定则提问。
存在多种解释时，列出方案，不静默选择。
存在更简方案时，指出并说明。
遇到模糊之处，停止，明确命名困惑点，直接Ask User Question。

二、简洁第一
最小代码解决问题，无推测性内容。
不添加超出请求范围的功能
单次使用的代码不抽象
不添加未请求的"灵活性"或"可配置性"
不处理不可能发生的场景
能用 50 行则不用 200 行
自问："高级工程师会说这过于复杂吗？" → 是则简化。

三、精准变更
只触碰必须改动的，只清理自己产生的。
修改现有代码时：
不"改进"相邻代码、注释或格式
不重构未损坏的部分
匹配现有风格，即使你会有不同写法
发现无关死代码：提及但不删除
变更产生孤儿时：
移除本次变更导致未使用的 import/变量/函数
不移除先前存在的死代码，除非被要求
检验标准：每行变更都应追溯到用户请求。

四、目标驱动
定义成功标准，循环验证。
将任务转化为可验证目标：
"添加验证" → "为无效输入写测试，然后使其通过"
"修复 bug" → "写测试复现，然后使其通过"
"重构 X" → "确保测试变更前后都通过"
多步骤任务简述计划
强成功标准支持独立循环；弱标准（"让它工作"）需持续确认。

五、执行规则
回答前通过合适工具确认文件最新状态，不依赖过期记忆。

展示代码时确保完整可复用。复杂操作分步执行，每步完成后简要同步状态。

中文沟通

回答简洁直接，不过度解释

六、变更总结
完成代码修改任务后，使用中文总结：

修改了什么 - 具体变更内容
为何这样修改 - 修改原因和选择依据
变更的意义 - 对项目/功能的影响
总结文件保存至：openspec/change/ 目录