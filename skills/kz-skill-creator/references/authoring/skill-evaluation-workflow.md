---
name: skill-evaluation-workflow
description: Skill 结构评估工作流参考。当用户要求评价、审查、点评一个已有 Skill 或相关可复用指令资产，并输出结构化评估报告时使用此参考。
version: 1.3.4
---

# Skill / 指令资产结构评估工作流参考

<!-- @类型: 参考指南 -->
<!-- @目的: 指导已有 Skill 或相关可复用指令资产的结构评估、证据收集、问题归纳与评估文档输出 -->
<!-- @场景: 用户要求评价、审查、点评已有 Skill / 指令资产，或要求把评估结果保存成文档 -->
<!-- @触发条件: 当前任务目标是先判断一个 Skill 或指令资产的结构质量、可维护性与重构优先级，而不是立即改写它 -->

> **一句话**: 先收集证据、再跑校验、后下结论，最后把评估结果沉淀成可复用文档
> **版本**: v1.3.4
> **用途**: 指导 Skill / 指令资产结构评估与评估报告写作
> **适用范围**: 目标 Skill 或相关可复用指令资产的结构质量审查、重构优先级判断与文档化反馈

## 目录

- [1. 文档定位](#1-文档定位)
- [2. 推荐证据范围](#2-推荐证据范围)
- [3. 评测工作流](#3-评测工作流)
- [4. 默认报告结构](#4-默认报告结构)
- [5. 常见结构问题](#5-常见结构问题)
- [版本历史](#版本历史)

---

## 1. 文档定位

### @步骤1: 先确认这是不是“结构评估”任务

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @验证点: 已确认当前任务目标是评价已有 Skill 或相关可复用指令资产，而不是直接改写或发布它 -->
<!-- @验证方式: 能说明这次任务的交付物是评估报告、重构建议，还是直接修改目标 Skill -->
<!-- @ID: step-confirm-evaluation-task -->

这份参考负责的是：

- 评价一个已有 Skill 或相关可复用指令资产当前写得怎么样
- 找出结构硬伤、主文档职责问题、指令边界问题、references 分层问题与验证缺口
- 给出优先级清晰的重构建议
- 产出可保存、可复用的评估文档

这份参考不负责的是：

- 不替代从零创建 Skill 的流程
- 不默认直接改写目标 Skill
- 不替代自动化 `eval-loop`

@提示: 如果用户已经明确要求“直接重构这个 Skill / 指令资产”，先做结构评估，再决定是否进入 [skill-refactoring-workflow.md](skill-refactoring-workflow.md)。

---

## 2. 推荐证据范围

### @步骤2: 先盘点应该看哪些文件

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @验证点: 已列出本次评估的证据文件范围 -->
<!-- @验证方式: 输出证据清单，至少覆盖主文档与关键 companion files -->
<!-- @ID: step-plan-evaluation-evidence -->

默认至少检查：

- 若目标是 Skill 目录：`SKILL.md`
- 若目标是独立指令资产：主指令文档本体（如 workflow prompt、agent instruction、评测模板）
- `README.md` 或同类说明文档（若存在）
- 关键 companion files：如 `checklist.md`、`examples.md`、`templates/`、`references/`
- `scripts/validate_skill.py` 或其他工程硬校验入口（若存在）
- `python scripts/skill_cli.py validate <skill-dir>` 的校验输出（仅当目标是 Skill 目录时）

如果目标 Skill 体量较大，先建立“最小必读证据清单”，避免一次性加载全部内容。

@提示: 目标是“证据够支持结论”，不是“把所有文件都读完”。

---

## 3. 评测工作流

## @工作流: 评测已有 Skill / 指令资产 并输出评估文档

<!-- @类型: 主工作流 -->
<!-- @目的: 为已有 Skill 或相关可复用指令资产提供一条稳定的结构评估、问题归纳与文档化输出路径 -->
<!-- @场景: 用户要求评价、点评、审查一个 Skill / 指令资产，并希望得到书面结论或重构建议时 -->
<!-- @前置条件: 已拿到目标 Skill 路径或主要文件内容 -->
<!-- @后置验证: 已产出带证据的评估报告，明确优点、问题、优先级与后续方向 -->
<!-- @触发条件: 当前任务目标是评价已有 Skill 或指令资产的结构质量，而不是直接进入实现修改 -->
<!-- @ID: wf-evaluate-existing-skill-and-save-report -->

### @步骤3: 先确认输出约束与保存路径

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @验证点: 已明确报告是否需要保存，以及命名、语气和暴露信息约束 -->
<!-- @验证方式: 记录评估文档路径、格式要求和特殊措辞约束 -->
<!-- @ID: step-confirm-report-constraints -->

统一确认时，至少问清或推断：

- 是否需要保存成文档
- 评估文档默认落在哪个路径
- 是否需要隐藏本技能名称
- 是否需要使用第一人称、团队口径或用户指定代称

- @动作: 若用户要求保存评估结果，Skill 目录默认写入 `<target-skill>/skill-evaluation.md`；单文件指令资产默认写入同目录的 `<target-file-stem>.evaluation.md`
- @动作: 若用户要求隐藏本技能名称，在报告 frontmatter 与正文中用用户指定称呼替换；未指定时可使用“我”
- @动作: 若用户明显是在请求评测，但输入约束不完整，优先根据目标路径、是否保存、命名隐藏和评估重点自行补齐；只有存在高风险歧义时再集中确认

### @步骤4: 先读主文档，再按目标类型读 companion files

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @依赖: step-plan-evaluation-evidence -->
<!-- @验证点: 已基于主文档和关键 companion files 建立足够证据 -->
<!-- @验证方式: 输出证据来源清单，并说明每份文件为什么要读 -->
<!-- @ID: step-read-skill-evidence -->

推荐顺序：

1. 若目标是 Skill 目录，先看 `SKILL.md`；若目标是独立指令资产，先看主指令文档
2. 若目标 Skill 存在 `README.md` 或同类说明文档，再看它
3. 再根据主文档引用关系或实际文件结构，按需读取关键 companion files

当你发现用户请求缺了保存路径、命名隐藏或评估重点等约束时，优先按默认规则处理：Skill 目录保存到 `<target-skill>/skill-evaluation.md`，单文件指令资产保存到同目录的 `<target-file-stem>.evaluation.md`，命名隐藏按用户原话替换，评估重点以结构质量、可维护性和验证闭环为主。

@提示: 若目标 Skill 或主指令文档已经把“结构评测”前置为运行期入口，优先沿主文档给出的 reference 入口取证，不要把 `README.md` 当成必有文件。
@警告: 不要默认把所有 examples 全量读入上下文，尤其是大型示例库。

### @步骤5: 能跑 validate 时先跑 validate，再看人工结构问题

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @依赖: step-read-skill-evidence -->
<!-- @验证点: 已拿到结构校验结果，并区分硬错误与人工判断问题 -->
<!-- @验证方式: 运行 validate，并把输出中的关键错误纳入评估证据 -->
<!-- @ID: step-run-validate-before-judgment -->

若目标是 Skill 目录，统一先跑：

```bash
python scripts/skill_cli.py validate <path_to_skill_folder>
```

必要时再补：

```bash
cd <path_to_skill_folder>
python scripts/validate_skill.py --skill .
```

若目标不是 Skill 目录，不要为了形式统一而强行套 `skill_cli.py validate`；改为：

- 明确记录“当前目标不适用 Skill 目录级 validate”
- 盘点是否存在等价脚本、lint、模板校验或引用一致性检查入口
- 若没有适用硬校验，明确把这点写入评估文档的 `limitations` 或验证缺口

- @动作: 把 `validate` 输出中与 frontmatter、语义化标记、版本、链接、版本历史有关的错误写入证据
- @动作: 若目标不是 Skill 目录，把“为什么没有跑 validate、是否存在替代校验”写入证据
- @动作: 区分“校验器已证明的问题”和“基于结构口径的判断问题”

### @步骤6: 用结构口径判断复杂度、优点与主要问题

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @依赖: step-run-validate-before-judgment -->
<!-- @验证点: 已完成复杂度判断，并归纳主要优点与核心问题 -->
<!-- @验证方式: 输出复杂度结论、优点列表，以及按影响程度排序的问题清单 -->
<!-- @ID: step-judge-complexity-strengths-and-risks -->

评估时重点看，并统一压缩为 8 个主维度：

| 维度 | 权重 | 重点关注 |
| --- | ---: | --- |
| 目标与适用边界 | 15% | 目标是否清楚、触发边界是否稳定、是否容易误分流 |
| 指令一致性与精准性 | 15% | 指令是否支持目标，是否存在模糊词、冲突或隐含假设 |
| 输入输出契约 | 15% | 输入要求、输出结构、缺失输入处理是否清楚 |
| 工作流完整性 | 15% | 是否存在稳定可执行的最小步骤链与默认动作 |
| 鲁棒性与安全边界 | 15% | 对异常、缺失材料、越界执行和风险场景的处理是否稳健 |
| 可维护性与渐进披露 | 10% | 主文档是否仍是路由层，references 是否分层清晰，是否重复维护 |
| 可验证性与测试闭环 | 10% | 是否有 `validate`、脚本入口、回归验证或评测闭环 |
| Token 效率与冗余控制 | 5% | 是否存在大段重复、低价值说明和可压缩冗余 |

- @动作: 每个维度单独打 `0-100` 分，并写 1 句主要扣分原因
- @动作: 最终总分按上述权重加权，得到 `总分 / 100`
- @动作: 问题排序优先看影响：先写会影响触发、执行、验证或长期维护的问题，再写局部表达优化

### @步骤7: 产出结构化评估报告

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @依赖: step-judge-complexity-strengths-and-risks -->
<!-- @验证点: 已产出结构化评估报告，并保存到目标路径 -->
<!-- @验证方式: 检查文档包含固定章节、证据引用和后续建议，且已写入目标路径 -->
<!-- @ID: step-write-evaluation-report -->

默认不要直接重写目标 Skill / 指令资产，除非用户明确要求。评估阶段的主要交付物应是评估文档。

- @动作: 使用本参考第 4 节的默认报告结构
- @动作: 报告中必须包含 8 维评分表、维度权重和加权总分
- @动作: 在报告中引用关键文件和必要的校验输出
- @动作: 给出“下一步应该重构什么、先改哪里”的顺序

---

## 4. 默认报告结构

### @步骤8: 用固定结构写评估文档

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @验证点: 评估文档结构稳定、可复用、可继续迭代 -->
<!-- @验证方式: 检查是否包含以下固定章节 -->
<!-- @ID: step-use-default-evaluation-report-structure -->

默认采用以下结构：

1. `评估范围`
2. `一句话结论`
3. `复杂度判断`
4. `8维评分表`
5. `主要优点`
6. `主要问题`
7. `工作流重构建议`
8. `优化建议`
9. `建议的重构方向`
10. `总评`

默认要求：

- frontmatter：`title`、`date`、`evaluator`、`framework`、`target`；建议补 `scope`、`limitations`
- `8维评分表` 必须使用以下 8 个维度，并统一显示“维度分 / 100”“权重”“主要扣分依据”
- `主要问题` 按优先级排序，并优先覆盖结构性硬伤
- 若跑过 `validate`，应写出关键结论；若出现硬错误，应显式落到 `主要问题`
- 若目标不是 Skill 目录，`target` 可填写主指令文档路径或目标对象名称，并在 `limitations` 里写清适用的校验边界

默认评分表字段：

| 维度 | 维度分 / 100 | 权重 | 主要扣分依据 |
| --- | ---: | ---: | --- |
| 目标与适用边界 |  | 15% |  |
| 指令一致性与精准性 |  | 15% |  |
| 输入输出契约 |  | 15% |  |
| 工作流完整性 |  | 15% |  |
| 鲁棒性与安全边界 |  | 15% |  |
| 可维护性与渐进披露 |  | 10% |  |
| 可验证性与测试闭环 |  | 10% |  |
| Token 效率与冗余控制 |  | 5% |  |

- 加权总分：`总分 / 100`
- 计算方式：各维度 `0-100` 分分别乘以权重后求和
- 输出要求：总分后必须补一句“主要扣分原因”，避免只给数字不给依据

默认映射建议：

| 加权总分 / 100 | 默认推荐重构方案 |
| --- | --- |
| `80-100` | 轻量修订 |
| `60-79` | 结构重整 |
| `<60` | 深度重构 |

- @动作: 先按总分区间给出默认推荐重构方案，再结合问题密度、单点致命风险和材料完整度判断是否需要覆盖默认映射
- @动作: 若覆盖默认映射，必须在报告中写明“为什么这次不按默认区间走”

- @动作: 生成正式评估文档时，优先复制 [skill-evaluation-template.md](../templates/skill-evaluation-template.md) 作为骨架，再按本 workflow 回填证据和结论
- @动作: 若希望让 8 维分数更稳定、可复核，先过一遍 [skill-evaluation-checklist.md](../templates/skill-evaluation-checklist.md)，再回填正式评分表
- @动作: 若用户只需要简版结论，可在不删除 `8维评分表` 的前提下，压缩模板中的低优先级建议与说明文字

按需增强：

- 文件证据链接
- `validate` 的关键输出片段
- companion files 的职责证据
- `建议的重构方向`

@提示: 若用户要求隐藏本技能名称，可将 `framework` 写成用户指定称呼；未指定时可写 `我`。
@提示: 默认只维护这一套标准 `skill-evaluation.md` 结构，不额外拆出多套报告格式。

---

## 5. 常见结构问题

### @步骤9: 优先抓“结构性硬伤”

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @验证点: 已优先识别最影响结构稳定性的高优先级问题 -->
<!-- @验证方式: 检查问题列表是否先覆盖以下高频项 -->
<!-- @ID: step-check-common-high-priority-issues -->

高频结构问题通常包括：

- Skill 目录缺失 frontmatter，或独立指令资产缺少最小元信息与用途声明
- 主文档没有采用语义化标记体系
- 主 `SKILL.md` 过厚，不再像路由层
- `README.md` 与主文档重复维护
- companion files 存在明显职责重叠
- references 很大，但主文档没有说明“何时读哪份”
- 没有明确验证入口，或只写了描述没有真正脚本入口

@提示: 这些问题的核心不是“写得好不好看”，而是“结构是否稳定、是否能长期维护、是否能被验证”。

---

## 版本历史

- **v1.3.4** (2026-05-17) - 补充默认报告落点规则：为单文件指令资产新增同目录 `<target-file-stem>.evaluation.md` 默认输出路径，避免继续误用 `<target-skill>/skill-evaluation.md`
- **v1.3.3** (2026-05-17) - 对齐评测边界到“Skill / 指令资产”：补齐独立指令资产的取证与校验规则，明确非 Skill 目标时如何记录 `validate` 限制
- **v1.3.2** (2026-05-17) - 接入 8 维量化 checklist：新增内部复核清单入口，用于在正式评分前稳定 8 个维度的打分依据
- **v1.3.1** (2026-05-17) - 补齐“总分区间 -> 默认推荐重构方案”映射，并要求在覆盖默认建议时写明理由
- **v1.3.0** (2026-05-17) - 将内嵌评估模板正式抽离到 [skill-evaluation-template.md](../templates/skill-evaluation-template.md)，workflow 只保留结构要求与引用入口，减少重复维护
- **v1.2.1** (2026-05-17) - 将 8 维评分表正式合并进可直接复制的评价模板，补齐 `skill-evaluation.md` 的 Markdown 骨架
- **v1.2.0** (2026-05-17) - 吸收 `reports/` 的 8 维评测口径：将默认评估维度统一为 8 项，每项单独按 `0-100` 打分，并按权重汇总为总分 `/100`
- **v1.1.0** (2026-05-17) - 对齐主文档与基础口径：将 README 调整为“存在时再读”，并补充“优先沿主文档评测入口取证”的说明
- **v1.0.0** (2026-05-13) - 初始版本，沉淀已有 Skill 的结构评估、证据收集、问题归纳与评估文档输出流程
