---
name: scenario-input-template
description: 场景输入模板参考。当目标 Skill 存在高频复用场景时，用它沉淀最有效的输入模板，方便后续直接复用。
version: 1.8.0
---

# 场景输入模板

<!-- @类型: 参考模板 -->
<!-- @目的: 提供围绕使用场景沉淀的可复制输入模板，帮助目标 Skill 稳定获得高质量输入 -->
<!-- @场景: 目标 Skill 存在高频复用场景，且这些场景逐步形成稳定、高效的提问模板 -->
<!-- @触发条件: 当目标 Skill 需要把常见场景的最佳输入方式沉淀为可复用模板时 -->

> **一句话**: 这是按“使用场景”沉淀的输入模板，不是工作流说明书
> **版本**: v1.8.0
> **用途**: 作为 `references/examples/*.md` 的编写基线
> **适用范围**: 需要沉淀高频场景输入模板的目标 Skill

## 使用原则

- 先从“用户常见使用场景”出发组织模板，而不是从工作流步骤出发
- 模板关注三件事：当前场景与目标、为达成目标提供哪些信息、最后希望拿到什么结果
- 目标 Skill 可以根据“目的”自动或半自动匹配到对应工作流，但模板本身不负责工作流路由
- 每个高频场景默认维护 1 个主模板；只有存在稳定分支时，才扩展到 2-3 个模板
- 模板文档本身也要维护版本：frontmatter `version`、头部 `> **版本**`、文末 `## 版本历史` 三处保持一致

## 固定结构

1. `适用场景`
2. `建议提供的信息`
3. `可直接复制输入模板`
4. `裁剪规则（可选）`

## 可直接复制输入内容

```markdown
# <场景名称> 输入模板

> **用途**: 用于 `<场景名称>` 这个高频使用场景，帮助一次性给出高质量输入
> **边界**: 本文档不负责工作流路由；工作流应由目标 Skill 根据“目的”和“提供的信息”判断

## 适用场景

- 场景名称: `<场景名称>`
- 一句话需求: `<用一句话概括这次请求，例如：创建一个处理 PDF 的 Skill>`
- 我这次想做什么: `<一句话写清本次目标，例如：帮我完成专利价值评估并给出结论>`
- 我最关注什么: `<列出 1-3 个重点，例如：判断依据清楚、风险说透、输出能直接复用>`
- 我希望最终得到什么结果: `<说明期望产物，例如：结论摘要 + 核心判断 + 后续建议>`

## 建议提供的信息

- 背景信息: `<这件事为什么要做、现在处于什么阶段、是谁在使用结果>`
- 现有材料: `<已整理好的文档、数据、链接、截图、草稿或历史记录>`
- 关键约束: `<时间、格式、篇幅、保密要求、不能触碰的边界>`
- 判断标准: `<你希望如何判断结果好不好，例如：准确、完整、可执行>`
- 参考样例（可选）: `<你认可的成品样式、参考案例或希望模仿的风格>`
- 其他补充（可选）: `<暂时没归类但又可能影响判断的信息>`

## 可直接复制输入模板

我现在要处理的场景是：`<场景名称>`。
这次希望你直接帮我完成：`<这次想做什么>`。
我最关注的是：`<我最关注什么>`。
我希望最终拿到的结果是：`<我希望最终得到什么结果>`。

为避免你来回追问，我先把关键信息一次性给你：

- 背景信息：`{补充当前背景、所处阶段、结果给谁使用}`
- 现有材料：`{补充已有文档、数据、链接、截图、草稿或历史记录}`
- 关键约束：`{补充时间要求、输出格式、保密边界或不能突破的限制}`
- 判断标准：`{补充你会如何判断这次结果是否足够好}`
- 参考样例（可选）：`{补充认可的样例、成品或风格}`
- 其他补充（可选）：`{补充其他可能影响判断的信息}`

如果信息已经足够，请直接进入最合适的处理流程；不要先给我解释有哪些工作流，除非你确实无法判断。
```

## 裁剪规则（可选）

- 某些场景不需要所有字段时，可以直接删掉不重要的字段
- 如果差异主要来自目标不同，优先保留同一场景模板，把差异写进 `适用场景` 里的目标描述
- 只有当场景本身稳定分化为不同输入习惯时，才拆成多个模板

## 落地建议

- 固定模板来源保留在 `references/templates/`，例如 `references/templates/scenario-input-template.md`
- 自动生成的场景示例文档写到 `references/examples/input-template-<english-slug>.md`
- 目标 Skill 需要新增模板时，优先复制上面的“可直接复制输入内容”再做最小改写
- 模板标题与内容都优先体现“场景名”，不要直接把模板写成工作流名
- 可用命令：`python scripts/skill_cli.py generate-templates <skill-path>`，基于 `SKILL.md` 的工作流自动生成场景示例草稿，并更新 `references/examples/index.md`
- 若覆盖已有模板，建议递增补丁版本，并把最新变更写入 `## 版本历史` 首条；版本历史最多保留 5 条

## 版本历史

- **v1.8.0** (2026-05-02) - 优化可复制提示词占位：统一改为 `{...}` 风格，便于识别这里需要用户补充
- **v1.7.0** (2026-05-02) - 强化示例命名与结构：示例文件名明确为 `input-template-<english-slug>.md`，并将“典型请求/使用时机”收敛为“一句话需求”
- **v1.6.0** (2026-05-02) - 优化模板文案：用途改为面向 `references/examples/`，示例文件名占位简化为 `input-template-<slug>.md`，并补强目标与信息字段提示
- **v1.5.0** (2026-05-02) - 调整场景模板结构：将“这次目的”合并进“适用场景”，统一示例命名为 `input-template-<scenario-slug>.md`
- **v1.4.0** (2026-05-02) - 修正路径职责：`templates/` 仅保留固定模板来源，自动生成的场景示例文档改回写入 `references/examples/`
