---
name: skill-evaluation-template
description: Skill 结构评估报告模板。用于为已有 Skill 或相关可复用指令资产输出标准化评估文档时直接复制使用。
version: 1.1.2
---

# Skill 结构评估报告模板

<!-- @类型: 参考模板 -->
<!-- @目的: 为已有 Skill 或相关可复用指令资产的结构评估输出提供一份稳定、可复制、可继续裁剪的标准模板 -->
<!-- @场景: 已完成证据收集、validate 或等价校验与问题归纳，准备输出 `skill-evaluation.md` 或同类评估文档 -->
<!-- @触发条件: 当需要把评估结论沉淀成正式文档，而不是只给口头建议时 -->

> **一句话**: 这是评估报告的成品模板，不负责工作流路由，只负责把结论稳定写出来
> **版本**: v1.1.2
> **用途**: 作为 `skill-evaluation.md` 的默认骨架，统一章节、评分表与重构建议写法
> **适用范围**: 已有 Skill 或相关可复用指令资产的结构质量评估、问题归纳、重构优先级判断与文档化输出

## 1. 使用原则

### @步骤1: 保留原评价体系骨架，再补强评分和结论字段

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @验证点: 模板既保留原有 `kz-skill-creator` 的章节顺序，又吸收 reports 中高价值的评分与建议字段 -->
<!-- @验证方式: 检查模板是否同时包含“评估范围 / 复杂度判断 / 主要问题 / 总评”和“8维评分表 / 总分 / 推荐重构方案” -->
<!-- @ID: step-keep-original-structure-and-absorb-report-fields -->

- @动作: 保留原始评价体系的 10 个核心章节，不把报告改写成完全不同的审计体裁
- @动作: 吸收 `reports/` 中更成熟的字段：`评估范围与限制`、`8维评分表`、`加权总分`、`推荐重构方案`
- @动作: 默认用自然语言解释扣分原因，不强制使用问题标签体系
- @动作: 除非用户明确要求，不在评估报告中直接附“优化后版本”，先把评估和修改分开

默认映射口径：

| 加权总分 / 100 | 默认推荐重构方案 | 默认解释 |
| --- | --- | --- |
| `80-100` | 轻量修订 | 核心结构成立，问题主要集中在局部补丁、措辞、少量边界或链接修复 |
| `60-79` | 结构重整 | 核心目标仍稳定，但主文档职责、workflow 边界、references 分层或验证入口已有明显失衡 |
| `<60` | 深度重构 | 边界、执行链或目录结构已经不稳，继续局部修补的收益明显下降 |

@提示: 这是默认映射，不是硬编码规则。若总分较高但存在单点致命问题，或总分较低但只是材料不完整，应允许基于证据覆盖默认建议。

## 2. 可直接复制模板

### @步骤2: 直接从这个骨架生成正式评估文档

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @验证点: 模板可直接复制使用，且已经包含默认 frontmatter、章节与 8 维评分表 -->
<!-- @验证方式: 复制后只需替换占位符即可形成完整评估文档 -->
<!-- @ID: step-copy-direct-evaluation-template -->

```markdown
---
title: <评估标题>
date: YYYY-MM-DD
evaluator: <评估者>
framework: <框架名称或用户指定称呼>
target: <目标 Skill / 指令资产路径或名称>
scope: <本次评估覆盖文件或范围>
limitations: <缺失材料、未覆盖范围或主要假设；若无可写“无”>
---

# Skill 评估报告

## 1. 评估范围

- 目标对象：
- 本次检查文件：
- 主文档类型：`SKILL.md` / 独立指令文档 / 其他
- 是否检查关键 companion files：
- 本次是否运行 `validate`：
- 是否存在等价硬校验入口：
- 评估边界与限制：

## 2. 一句话结论

- 结论：
- 是否能稳定达成目标：可以 / 部分可以 / 不可以
- 加权总分：`<总分> / 100`
- 推荐重构方案：轻量修订 / 结构重整 / 深度重构

## 3. 复杂度判断

- 复杂度：轻量 / 中等 / 复杂
- 判断依据：
- 当前最主要的结构风险：

## 4. 8维评分表

| 维度 | 维度分 / 100 | 权重 | 主要扣分依据 |
| --- | ---: | ---: | --- |
| 目标与适用边界 |  | 15% |  |
| 指令一致性与精准性 |  | 15% |  |
| 输入输出契约 |  | 15% |  |
| 工作流完整性 |  | 15% |  |
| 鲁棒性与安全边界 |  | 15% |  |
| 可维护性与渐进披露 |  | 10% |  |
| 可验证性与测试闭环 |  | 10% |  |
| Token 效率与冗余控制 |  | 5% |  |

- 加权总分：`<总分> / 100`
- 总分主要扣分原因：
- 推荐重构方案判断依据：`<默认按总分区间判断；若覆盖默认映射，说明覆盖理由>`

## 5. 主要优点

1.
2.
3.

## 6. 主要问题

### 问题 1

- 位置：
- 问题：
- 影响：
- 证据：
- 建议：

### 问题 2

- 位置：
- 问题：
- 影响：
- 证据：
- 建议：

### 问题 3

- 位置：
- 问题：
- 影响：
- 证据：
- 建议：

## 7. 工作流重构建议

- 是否建议调整 workflow：
- 是否建议补“决策矩阵 + 强规则摘要”：
- 是否建议下沉内容到 `references/`：
- 不建议现在就动的部分：

## 8. 优化建议

### 高优先级
1.
2.
3.

### 中优先级
1.
2.
3.

### 低优先级
1.
2.
3.

## 9. 建议的重构方向

- 推荐顺序：
- 先改哪里：
- 为什么：
- 修改后最小验证建议：

## 10. 总评

- 总评：
- 稳定性判断：高 / 中 / 低
- 优化优先级：高 / 中 / 低
- 后续建议：
```

## 3. 填写说明

### @步骤3: 让模板既稳定又不过度机械

<!-- @类型: 操作步骤 -->
<!-- @优先级: 必须 -->
<!-- @验证点: 报告内容充分但不僵硬，能说明分数、问题和建议之间的关系 -->
<!-- @验证方式: 任一主要问题都能回指到证据，任一分数都能解释主要扣分原因 -->
<!-- @ID: step-fill-template-with-evidence -->

- @动作: `scope` 与 `limitations` 建议保留，因为 `reports/` 的结论明确要求评估报告说明覆盖范围与限制
- @动作: `8维评分表` 中每个分数都至少写 1 句扣分依据，不要只填数字
- @动作: `主要问题` 优先写影响触发、执行、验证或长期维护的问题，再写表达层问题
- @动作: `推荐重构方案` 与 `优化优先级` 要能被前面的分数和问题支撑，避免拍脑袋判断
- @动作: 可先按总分区间给出默认推荐重构方案，再根据是否存在致命单点问题、材料缺失或用户硬约束决定是否覆盖默认映射
- @动作: 若用户要求隐藏本技能名称，可将 `framework` 填成用户指定称呼或“我”
- @动作: 若目标不是 Skill 目录，在 `评估范围` 中写清主文档类型，并在 `limitations` 中明确说明为何没有或无法运行 `skill_cli.py validate`
- @动作: 若目标是单文件指令资产，默认可将成品文档落到同目录的 `<target-file-stem>.evaluation.md`，除非用户指定了其他路径

## 4. 裁剪规则

### @步骤4: 只裁剪低价值字段，不裁剪关键判断信息

<!-- @类型: 操作步骤 -->
<!-- @优先级: 可选 -->
<!-- @验证点: 在压缩篇幅时仍保留范围、评分、问题和结论四类关键信息 -->
<!-- @验证方式: 检查裁剪后仍能看出“评了什么、为什么这么评、先改什么” -->
<!-- @ID: step-trim-template-safely -->

- @动作: 若用户只要简版，可压缩 `主要优点`、`低优先级建议` 和长说明，但不要删除 `8维评分表`
- @动作: 若目标 Skill 很轻量，`问题 3` 可删掉，`中 / 低优先级` 建议也可合并
- @动作: 若没有实际运行 `validate`，必须在 `评估范围` 和 `limitations` 中明确说明
- @动作: 若用户明确只要结构评估，不要擅自追加“优化后版本”

## 版本历史

- **v1.1.2** (2026-05-17) - 收口单文件目标的模板口径：将“生成 `skill-evaluation.md`”改为生成正式评估文档，并补充 `<target-file-stem>.evaluation.md` 的默认落点
- **v1.1.1** (2026-05-17) - 对齐适用范围到“Skill / 指令资产”：补充目标对象、主文档类型与等价硬校验入口字段，避免非 Skill 目标误套 `validate`
- **v1.1.0** (2026-05-17) - 补齐“总分区间 -> 默认推荐重构方案”映射，并在模板正文与可复制骨架中加入推荐方案判断依据字段
- **v1.0.0** (2026-05-17) - 首次从 `reports/` 与 `kz-skill-creator` 原评价体系融合出独立评估模板，统一 8 维加权评分、报告章节与重构建议字段
